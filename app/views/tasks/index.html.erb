<h1>Tasks</h1>

<%= link_to "New task", new_task_path %>

<% if @tasks.present? %>
  <ul>
    <% for task in @tasks %>
      <li><%= link_to task.name, task %></li>
    <% end %>
  </ul>
<% else %>
  <p>No tasks for this group</p>
<% end %>

